# --- Core UI ---
streamlit>=1.36,<2

# --- Data utilities ---
numpy
pandas
scikit-learn
pillow

# --- Face recognition ---
# setuptools must be < 70 because face-recognition-models uses the
# old `use_2to3` distutils flag.
setuptools<70.0.0
cmake
dlib
face-recognition
face-recognition-models
opencv-python-headless

# --- Database / auth ---
supabase>=2.5,<3
bcrypt

# --- QR codes (subject sharing) ---
segno

# --- Voice recognition ---
librosa
resemblyzer

# --- Environment & Configuration ---
python-dotenv>=1.0.0

# --- Security & Logging (NEWLY ADDED) ---
python-json-logger>=2.0.0  # Structured logging

# --- Testing (NEWLY ADDED) ---
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
